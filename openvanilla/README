0. 第一次下載請執行

     `./init.sh`

   以取得 OpenVanilla 的程式碼

1. 編譯 OpenVanilla 輸入法模組，請執行
   
     `cd Modules`
     `make`

2. 安裝 
      
     `INSTALL_PREFIX=/usr/local/ make install` 

   其中 INSTALL_PREFIX 可以改成目的路徑
