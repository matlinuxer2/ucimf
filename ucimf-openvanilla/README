0. 第一次編譯請執行

     `autoreconf -i --force`

   以重新展開 configure 和 Makefile

1. 編譯 
     `OV_MODULEDIR=/lib/openvanilla ./configure ` 
     `make`

     其中 OV_MODULEDIR 可以換成 openvanilla 其他放模組的路徑

2. 安裝 
      
     `make install` 

