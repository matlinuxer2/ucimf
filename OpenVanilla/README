Please execute for first compilation

     `./init.sh`

The script will fetch source codes and build automaticallly,
and produce packages as 
      
      openvanilla-modules-VERSION.tar.gz  ( Source )
      openvanilla-modules-VERSION.tar.bz2 ( Binary )

